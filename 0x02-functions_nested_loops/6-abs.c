#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @c: takes a parameter to be passed
 * Return: always 0
 */

int _abs(int c)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
